import Link from 'next/link';import { supabase } from '@/lib/supabase';
export default async function Leads(){const {data}=await supabase.from('leads').select('*').order('created_at',{ascending:false});return <main className="wrap"><header className="nav" style={{margin:'-24px -24px 24px'}}><b>Leads</b><Link href="/admin">Panel</Link></header><div className="card"><table><thead><tr><th>Nombre</th><th>Email</th><th>Teléfono</th><th>Estado</th><th>Mensaje</th></tr></thead><tbody>{(data||[]).map((l:any)=><tr key={l.id}><td>{l.name}</td><td>{l.email}</td><td>{l.phone||'-'}</td><td>{l.status}</td><td>{l.message}</td></tr>)}</tbody></table></div></main>}
