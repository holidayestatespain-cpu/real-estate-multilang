import Link from 'next/link';
import { supabase } from '@/lib/supabase';
export default async function Admin(){const {data:{user}}=await supabase.auth.getUser();const {data:properties}=await supabase.from('properties').select('id,reference,title,location,price,status').order('created_at',{ascending:false});const {count}=await supabase.from('leads').select('*',{count:'exact',head:true});return <><header className="nav"><b>ADMIN · Holiday Estate Spain</b><Link href="/admin/properties">Propiedades</Link><Link href="/admin/leads">Leads</Link><Link href="/es">Ver web</Link></header><main className="wrap"><div className="card"><p className="muted">{user?.email||'Sesión activa'}</p><h1>Panel de administración</h1><div className="grid two"><div><h2>{properties?.length||0}</h2><p className="muted">Propiedades</p></div><div><h2>{count||0}</h2><p className="muted">Leads recibidos</p></div></div></div><h2>Últimas propiedades</h2><div className="card"><table><thead><tr><th>Referencia</th><th>Título</th><th>Ubicación</th><th>Estado</th></tr></thead><tbody>{(properties||[]).map((p:any)=><tr key={p.id}><td>{p.reference}</td><td>{p.title}</td><td>{p.location}</td><td>{p.status}</td></tr>)}</tbody></table></div></main></>}
